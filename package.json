{
  "name": "heartsync-app",
  "version": "2.0.0",
  "private": true,
  "description": "Khong gian rieng tu va real-time danh cho hai nguoi.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "node --check api/_firebase-admin.js && node --check api/_push.js && node --check api/couple.js && node --check api/message.js && node --check api/reminders.js && node --check shared/reminder-schedule.cjs && node --check public/firebase-messaging-sw.js && npm run check:runtime",
    "check:runtime": "node --no-experimental-require-module -e \"require('./api/couple.js'); require('./api/message.js'); require('./api/reminders.js')\"",
    "daily-encouragement": "node scripts/generate-daily-encouragement.mjs",
    "emoji-catalog": "node scripts/generate-emoji-catalog.mjs",
    "icons": "node scripts/generate-icons.mjs",
    "smoke:ui": "node scripts/ui-smoke.cjs",
    "test": "node --test tests/*.test.cjs",
    "test:rules": "npx --yes firebase-tools@13.35.1 emulators:exec --only auth,database --project demo-heartsync \"node scripts/rules-smoke.mjs\""
  },
  "engines": {
    "node": "24.x"
  },
  "dependencies": {
    "firebase": "12.16.0",
    "firebase-admin": "13.6.1",
    "lucide": "1.24.0"
  },
  "devDependencies": {
    "sharp": "0.35.3",
    "vite": "8.1.4"
  }
}
